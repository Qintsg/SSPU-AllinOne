# 清源设计系统变更记录

- 首页服务摘要排序 v01 完成设计与 Flutter 实现：常规设置将四个固定区域开关与四个可排序服务摘要拆为两段连续账本，培养方案、校园卡、邮箱和体育考勤在同一行同时提供上移、下移与显示开关；排序只改变首页摘要位置，隐藏不等于停止联网刷新。参考 UCSD `campus-mobile` 的可访问卡片排序路径，五端统一保留显式上下移动按钮，不把拖拽作为唯一操作。首页今日学程时间轨与行动坞顺序保持冻结；减少动态时绕过零时长 `AnimatedSize`，避免异步恢复顺序时布局重入。

- 教务详情恢复账本 v01 完成设计、评审与 Flutter 实现：共享加载、空与错误状态移除最小高度和居中海报式编排，改为图标—事实—唯一动作的左锚定连续记录；360px 使用紧凑 token 间距并让行动自然落行，768px 起将恢复行动与原因并入同一阅读行。成绩、考试、过程化成绩、体育和第二课堂的 service、缓存、筛选、路由、单飞锁与错误文案均保持不变。新增 360×800、1× DPR 考试错误恢复回归，固定状态卡小于 240dp、行动不小于 48dp 且能触发原重试回调；Windows 重采集考试详情六态四档亮暗 48 张候选并人工复核。全量 Flutter 测试、静态分析、设计/样式/五平台 CI 契约、Windows Debug 构建与 1544 PNG、144 sidecar 完整性校验均通过；全程跳过 SSIM。

- 校园资讯恢复列宽 v03 完成设计、评审与 Flutter 实现：1200px 无缓存错误不再把“无法刷新 + 重试”拉满内容列，改为最多 560dp、与标题左缘对齐的恢复列；360px 仍保持满宽纵向图标、说明和 48dp 行动，768px 起继续以图标—说明—行动同排呈现。初始、加载、未认证、陈旧缓存、筛选无结果、来源设置、搜索、分页和刷新单飞语义均保持不变。资讯视图拆为页面根、头部内容、来源控件和状态面板，9 项布局回归、浏览器参考 48 张、Windows 48 张候选和全量视觉目录完整性校验通过；全量 Flutter 回归 720 项（1,556 项视觉采集跳过）、静态分析、设计/CI 门禁与当前源码 Windows Release 构建通过；全程跳过 SSIM。

- 教务总览连续学程流 v03 与恢复状态密度 v01 完成设计、评审与 Flutter 实现：360px 标题改为完整“学习进度”，避免刷新按钮旁将“一处看全。”拆成孤字；768px 起保留完整短句。宽屏同排卡片改为顶部对齐、各自按内容结束，移除下一场考试与完成度被相邻卡片拉高的无信息空框；“查看详细数据源”与来源区恢复为连续阅读间隔。稿外复核发现凭据不足等无内容状态仍使用全宽、固定高卡片，遂另行冻结恢复状态稿，统一收束为最多 560dp 的内容高度恢复面板，保留账户连接、网络重试、学期调整、加载 live region 与操作锁。教务总览由 1,355 行拆为状态映射、页面布局、内容网格、事实卡片、状态面板和详细来源六个内部模块，公共入口与业务契约不变。教务回归 60 项、全量 Flutter 回归 719 项（1,556 项视觉采集跳过）、静态分析、设计系统/样式门禁与原型浏览器 80 张通过；Windows 重采集总览 10 态 × 四档 × 亮暗共 80 张并人工复核，全部 1544 张候选与 144 份 sidecar 通过完整性校验，当前源码 Windows Release 构建成功；按当前决定跳过 SSIM。

- 课程表紧凑端日期带 v01 完成设计、复审与 Flutter 实现：360/390px 不再为露出“今天”而截断前一个日期标签，改为完整可见的“周一…周五、今天、周日”七日带；视觉短标签与完整读屏标签分离，七个目标均保持至少 48dp，点击与左右方向键仍按一日切换。首版单字“⼀”候选经人工复审后改为两字周名，避免误读为分隔线；768px 以上仍保留全称页签和周网格。课程表控制器、状态面板与响应式视图拆分为 476/212/402 行文件，行为不变。课程表/组件回归 34 项、静态分析与差异检查通过，Windows 课程表 7 态四档亮暗候选 56/56 已重采集并人工复核；全程跳过 SSIM。

- 校园卡终端错误恢复条 v01 完成设计、评审与 Flutter 实现：无有效本地快照时，错误不再以居中高空卡呈现；360px 警告图标与标题同行、说明与“重试只读操作 / 返回首页”连续排列，768px 起收束为左锚定内容高恢复条。失败原因、凭据失效禁用重试、返回首页、单飞锁与不展示不可信余额/交易记录的边界保持不变。布局测试同步固定物理视口和 1× DPR，新增标题单行与双行动目标断言；校园卡/首页定向 57 项通过，Windows/Linux 各重采集 56 张并通过全量 1544 PNG、144 sidecar 完整性校验，Linux Debug 与真实 runner smoke 5/5 通过；全程跳过 SSIM。

- 校园卡空记录恢复条 v01 完成设计、评审与 Flutter 实现：本地日期/收支筛选无结果时，宽屏不再使用居中高空卡，改为与内容列左缘对齐、按内容高度结束的图标—说明—“清除筛选”恢复条；360px 保持紧凑纵向阅读与 48dp 行动目标。筛选恢复仍只调用本地 `_clearFilters`，余额、远端只读查询/同步、错误恢复与状态锁均不改变。校园卡/首页、安全、WebView 与校历定向行为回归 107 项、全量 Flutter 回归 713 项通过；校园卡详情四档亮暗 56 张在 Windows/Linux 重新采集并人工复核，两个平台当前 1544 张候选与 144 份 sidecar 完整性校验通过；Android 上一小批源码容器 Debug APK、Windows Release 和 Linux Debug/smoke 5/5 均通过；全程跳过 SSIM。

- 首页问候语短语换行 v01 完成设计与 Flutter 实现：360px 刷新按钮旁不再把“今天”拆为孤字，改为“早上好，/先看清今天。”两个完整阅读短语；768px 起自动恢复原有单行。标题字号、刷新 48dp 目标、首页无滚动预算、数据与页面动效均保持不变；原型 64 张、首页交互 6/6、Flutter 定向 57/57 及首页八态四档亮暗 64 张候选通过，按当前决定跳过 SSIM。

- Windows 当前清源视觉矩阵已在首页和邮件最新改动后按 193 个状态重新生成：`1544` 张 Flutter 候选 PNG 与 `144` 份外部区域 sidecar，采集 1544/1544 通过，文件名、尺寸、清单完整性和边界校验通过；同源码 Windows Release 与真实 runner 烟测 5/5 通过。该证据不替代 Android、iOS、macOS、Linux 的真实 runner 验收；按当前政策不计算 SSIM。

- Linux 当前源码已在 Flutter 3.44.0 容器中按 CI 同款原生依赖完成 Debug 构建、真实 runner 烟测 5/5、1544 张候选截图与 144 份 sidecar，产物完整性校验通过。Android、iOS、macOS 仍待对应真实 runner；按当前政策不计算 SSIM。

- 邮件撰写连续表单与提交坞 v01 完成设计与 Flutter 实现：删除卡内重复“撰写邮件”标题和冗余关闭图标；360px 紧凑端将取消/发送移至表单滚动区外的轻量提交坞，保持在底部导航上方可达；768px 以上继续使用内容高度表单与底部行动组。SMTP 请求、地址解析、失败上下文、取消、成功清空和发送单飞锁保持不变。新增紧凑端滚动可达和发送中全量锁定回归；四态、四档、亮暗共 32 张 Windows 候选经人工复核，按当前决定跳过 SSIM。

- 开源许可密度 v01 完成设计与 Flutter 实现：宽屏四列许可矩阵的外卡收束至实际内容宽度，移除 1200/1600 右侧无信息空框；移动项目卡、许可证数据、外链确认、失败恢复和单飞锁保持不变。许可回归 13 项、五态 Windows 目标候选 40 张、静态分析和设计门禁通过；全程跳过 SSIM。

- 校历证据栏密度 v01 完成设计与 Flutter 实现：1200px 以上的学期边界卡不再被 PDF 面板高度拉伸，左侧来源、查询、学年和边界事实各自按内容结束；右侧原始 PDF 继续独立填充外部阅读区。数据、PDF、单飞锁、错误恢复和 sidecar 均保持不变。校历/PDF 回归 22 项、校历全状态 Windows 目标候选 72 张、PDF 复核 64 张、静态分析和设计门禁通过；全程跳过 SSIM。

- 外观选择密度 v01 完成设计与 Flutter 实现：删除主题分段外层的全高卡片和水平滚动容器，正文改为内容高度、左锚定的单一主题选择线；三项主题、即时切换、保存回调、互斥语义、方向键和来源抽屉保持不变。针对性行为回归 5 项、四档亮暗 Windows 目标候选 8/8、静态分析和设计门禁通过；全程跳过 SSIM。

- 关于与法律阅读密度 v01 完成设计与 Flutter 实现：关于页三行构建账本在宽屏收束至表单阅读宽度并按内容高度结束；法律短预览不再填满视口，章节表面仅包住既有可读列，真实长文继续由页面自然滚动承载。保留返回、更多、外链确认、加载、错误重试、正文选择和全部业务契约；补齐三份法律文档的 loading/content/error 视觉夹具，使清单从 187 扩展为 193 个场景、五平台完整矩阵从 1496 更新为 1544 张。针对性行为回归 26 项、128 张 Windows 目标候选、静态分析和设计门禁通过；按最新决定跳过 SSIM，改用四档亮暗截图、操作协同与异常恢复核验。

- 首页可配置概览密度 v05 完成设计与 Flutter 实现：当用户关闭培养方案、校园卡、邮箱或体育摘要时，概览账本按 0/1/2/3-4 项收束，移除空外框、等高空卡和紧凑端多余行；无概览时行动坞紧跟时间轨，尺寸过渡使用 `motion.base` 并尊重减少动态。新增浏览器交互矩阵覆盖 360×800 与 1200×900 的全部、单项和全关闭场景，6/6 通过。
- 教务总览紧凑指标 v02 完成设计与 Flutter 实现：360px 下平均绩点、已获学分和已读成绩改为共享卡内的三列账本，保留等宽数字与数据语义；下一场考试和学习档案更早进入首屏，768px 以上继续保持原有横向比较。补充 360/768/1200/1600 四档亮暗候选，80/80 通过，修复滚动容器下紧凑 Row 的无限高度约束。
- 修复全量状态参考渲染器对 `partial-error`、`operation-locked`、凭据、外部确认和输入校验场景缺少通用文案兜底的问题；完整 Chromium 参考重新生成 1496 张 PNG 与 144 份外部区域 sidecar，浏览器门禁通过，全程跳过 SSIM。
- 新增 `settings.wechat-login` 微信公众号扫码登录完整视觉批次：loading、扫码待操作、成功、主页面失败、候选校验失败、认证提取锁定、外部确认和外部打开失败八态。设计采用“工具栏 + 认证接力条 + 填充式 WebView/恢复区”，移除重复页头和移动端无意义空白；Chromium 参考 64/64、Flutter 生产页面 Windows 候选 64/64，均跳过 SSIM，并补齐外部区域 sidecar。生产 `WxmpLoginPage` 保持默认 InAppWebView、Cookie 提取、候选回滚和业务服务契约不变，仅增加确定性视觉 fixture 入口。
- 微信扫码登录复审后补齐生产路径：共享 `WxmpLoginContentFrame` 让 fixture 只替换外部正文；loading/刷新锁定、initial 认证接力条、成功标题、旧 WebView generation 隔离和外部确认焦点循环均有行为测试，`WxmpLoginPage` 降至 662 行，预览模块 177 行。
- 数据与隐私危险确认从三段长文本重构为“将删除 / 仍会保留”本机边界账本，分别覆盖清除校园缓存、断开账户和清除全部本地数据；取消先聚焦，遮罩保持锁定，Escape、取消与系统返回均安全退出并把焦点归还触发入口，360×800 无需滚动。三个生产入口共享单一实现并保持原业务操作不变；设置模态参考脚本与 3259 行视觉采集测试已按领域拆分并增加加载链门禁。设计候选与 Windows 真实生产候选各 24/24 张通过，最新全量 HTML 参考与 Windows 矩阵均为 1496 PNG + 144 sidecar；Flutter 全量回归、Windows 原生 smoke 5/5、静态分析与 Release 构建通过，全程跳过 SSIM。

- 任务型模态框批次补齐本地密码与公众号高级配置的 content / validation-error 清单。公众号紧凑端保持四个认证字段单列，并将两个请求限制字段同行；宽屏改为行优先双列，视觉、语义与 Tab 顺序一致。视觉夹具同步修正根 Navigator/Overlay 仍使用默认 800×600 的问题，并移除关闭确认调用方与 `YhDialog` 的重复安全边距。v02 设计参考 16/16、最新全量 HTML 参考与 Windows Flutter 候选均为 1408 PNG + 104 sidecar，完整性门禁和人工关键屏复核通过；Flutter 全量 683 项、Windows 原生 smoke 5/5、静态分析与 Release 构建通过，全程跳过 SSIM。

- 全局反馈门面补齐 `motion.base` 淡入/短距离滑入与淡出，并显式透传减少动态设置；连续反馈仍替换上一条，3 秒可读时长和右上角紧凑布局保持不变。清源机器清单改为 `manual-per-screen`，拒绝遗留 SSIM 阈值；校验器拆分为 645 行主流程与 353 行清单/运行时模块。最新 HTML 参考和 Windows Flutter 3.44.0 候选已由后续任务型模态框批次扩展为 1408 PNG + 104 sidecar，浏览器门禁及文件名、尺寸、边界检查通过；未运行 SSIM。

- 后续五平台视觉验收按最新决定跳过 SSIM：CI 删除设计参考下载、数值比较和差异结果目录，继续严格生成并校验 176 状态 × 四档视口 × 亮暗主题的 1408 张平台候选，上传完整截图供人工逐屏评审；历史 v34 SSIM 记录仅作为既有证据保留，不再作为后续门禁。

- 全局紧凑反馈从旧 `widgets/app_feedback.dart` 归位到清源公共门面，统一使用 `showYhFeedback`，并删除运行时代码中的 `InfoBar` 旧术语和业务层设计子模块直连。新增静态门禁，禁止清源目录之外的 Dart 文件绕过 `qingyuan_ui.dart` 导入内部实现；门禁测试 37/37 与完整设计契约通过。反馈时长、替换策略、live region、尺寸、颜色与关闭动作保持冻结设计不变；Flutter 3.44.0 静态分析、定向 35 项与全量 680 项测试通过。

- 清源全量视觉参考 v34 纳入拆分样式加载链、失效 token 别名修订和五平台原生能力冒烟。Windows 参考与 Flutter 3.44.6 候选均为 1376 张 PNG、104 份外部区域 sidecar，逐图 1376/1376 通过；最低应用区 SSIM 0.900284，最低外部区 0.933080。独立 Flutter 3.44.0 工具链下的静态分析、680 项测试、Windows 原生冒烟 5/5、Release、1376 张候选采集与产物清单校验同样通过，按后续决定跳过重复 SSIM。Android、iOS、macOS、Linux 仍等待真实 CI runner，不能由 Windows 结果替代。

- 拆分后的 HTML/CSS 参考稿新增完整加载链与 token 引用门禁，修复教务排序/焦点和第二课堂规则动效中已失效的旧 token 别名；受影响的 Windows 定向矩阵分别为 80/80 与 16/16 逐图通过。五平台视觉任务在采集前新增临时目录、安全存储、系统认证、PDF、WebView 与桌面窗口真实 runner 冒烟；Windows 5/5 通过，其余平台等待对应 CI runner。

- 首页与通知 v17 将 360 的八项开关整理为连续 56dp 任务账本，移动端卡片底部留白从 32dp 收到 16dp；行标题显式绑定 600 字重 token，日历、信息、培养、校园卡、邮箱和链接图标恢复业务语义，44×26 可见开关轨道保留不小于 48dp 的命中区。四档亮暗设计稿通过浏览器门禁；Windows Flutter 8/8 张逐图通过，最低 SSIM 0.909569。

- 设置恢复状态完成内容高度收束：数据与隐私、应用更新和微信认证不再填满桌面剩余高度，360 状态横幅改用紧凑 supporting 排版，更新初始态直接呈现三行可操作账本。Windows 三页共 88/88 张逐图通过，最低 SSIM 分别为 0.943978、0.941961、0.940990；关于页七态共 56/56 张通过，最低 0.902140。

- 首页行动坞 v06 修正完整参考核验发现的 360px 触控目标不足：紧凑快捷区域移除重复的左右 8px 内边距，使三个入口从约 45.41px 恢复到不小于 48px，同时保留 4px 间隔、单层外框和 1.5:1.45 编排。该问题也补齐了前缀核验与全量 `_assert_targets` 覆盖差异的评审记录。

- 首页 v05 修正 v04 拆分后的 CSS 优先级缺陷：360px 行动坞真实采用 1.5:1.45 近等宽比例，“第二课堂”单行显示且三个快捷操作保持 48dp；768 使用 1:2，1200/1600 使用 2:3。Flutter 主区改为 42vh 并封顶 420dp，中等行动坞收至 80dp，快捷入口取消常驻描边并补齐键鼠、触控和减少动态反馈。原 1496 行首页视图拆为三个职责文件；Windows 八态、四档、亮暗共 64/64 张逐图通过，最低 SSIM 0.916481，全量 673 项测试、静态分析、设计门禁和 Windows Release 构建通过。其余四个平台仍待对应 CI runner。

- 首页密度与行动坞 v04 冻结：主区按视口高度约 42% 自适应并限制在 372–420dp，宽屏时间轨、概览行和区域间距不再随窗口拉伸；第二课堂与三个常用入口合并为单层行动坞，快捷入口取消常驻按钮描边但保留 48dp 目标、焦点、hover、按压和减少动态反馈。初始、加载、错误态删除重复内边距，陈旧、部分失败、凭据缺失和刷新锁定继续保留有效内容与恢复路径。八态、四档尺寸、亮暗主题共 64 张设计稿通过浏览器门禁和两轮目视自评审，冻结于 `build/design-review-home-density-v04`；Flutter 尚未实现本版，不能将设计通过表述为生产候选通过。

- 第二课堂积分规则从超宽网页矩阵重构为“完成差额 + 分类账本”：360 下优先显示未完成类别并将全部规则合并为单一连续账本，768 起使用左侧类别选择与右侧原位明细；点击、键盘方向键、48dp 目标和减少动态协同生效。空态保留来源与统一返回成绩单路径，必修、已获、积分、条件或状态缺失时明确说明来源不足，不推测为零。设计稿 `build/design-review-student-report-rules-v03` 经两轮自评审冻结；Windows 生产候选 `build/visual/windows-student-report-rules-v09` 的 16 张四档亮暗 content/empty 截图逐图通过，最低 SSIM 0.901557。Android、iOS、macOS、Linux 仍需对应 CI runner 验证。

- 安全与账户设置冻结为真实 8 分区壳内的连续工作台：密码保护、系统快速验证、凭据保存/验证和数据与隐私入口不再拆成重复卡片，360 下简单开关保持同行，768 起说明与动作同排，表单封顶 560dp。凭据状态读取失败会保留安全控制、输入与原连接，并提供原位重试；重复密码图标和“未填写”胶囊已移除，文字按钮保留 48dp 命中区但不再继承 96dp 实体按钮宽度。设计候选 `build/design-review-settings-account-v02` 的 16 张截图通过浏览器核验；Windows 真实生产页候选 `build/visual/windows-settings-account-v06` 为 16/16 逐图通过，最低 SSIM 0.902985。

- 修复主页时间轨暗色对比：当前事项不再把深色 `brandTint` 用在深蓝结构面上，统一使用 `onStructural` 并仅以字重强调；compact 辅助坞收至 8dp 间距并允许“第二课堂”两行完整显示。

- 设置原型恢复生产中的 8 个分区：360–899 使用分区抽屉，900–1199 使用顶部标签，1200 起使用左侧导航，解决 768 下最后分区被截断。常规分区改为“首页显示 → 消息推送 → 应用体验”，保留 8 项首页开关和所有存储语义，同时收起窗口、外观与更新的重复大卡；v07 继续去除应用栏下的重复标题、采用 56dp 行节奏和业务图标锚点，已通过四档亮暗和浏览器交互核验。

- 主页与首页设置密度稿 v0.2 冻结：主页固定首屏骨架通过四档亮暗、无滚动、无遮挡与 48dp 自动核验；增补部分来源失败、部分凭据缺失和刷新操作锁定状态。首页设置保留 8 项独立语义并分为“时间与学习 / 校园服务”，否决 3 个组合开关方案；compact 分区抽屉补齐遮罩层级、Escape、焦点循环和焦点归还。

- 主页密度与响应式进入新候选稿：从纵向卡片流调整为固定首屏骨架，新增四档视口高度预算与默认文字缩放无滚动要求；同层服务摘要改为统一概览面板，限制重复卡片、过大按钮外框和无信息留白。设置页新增三档分区导航与概览/高风险任务分离要求。视觉门槛继续按每张截图 SSIM ≥ 0.90，新设计质量与操作完整性优先于旧稿相似度；候选稿完成自评审前不得进入 Flutter 可见实现。

- 校历与 PDF 重构为“统一学期上下文 + 公开档案 + 原始证据”流程：同页分别标注当前实际学期/周次与查询使用学期，查询设置写入失败时保留原值并原位重试；公开档案显示模型中的真实抓取时间，紧凑端完整保留特殊日期和待定通知。刷新、查询学期、外部打开、PDF 下载与正文重试遵循共享操作锁，服务、来源和页面生命周期换代后旧结果不得回写；嵌入式加载失败不再暴露底层异常或绕过外部确认。PDF 视觉 fixture 改由真实 `AcademicCalendarPdfPage` 驱动，确认、取消、拒绝、部分失败与锁定均从生产动作进入。视觉清单扩展为 169 个状态、1352 张完整设计参考和 104 份外部区域 sidecar；Windows 本批 136/136 张逐图达到 SSIM 0.90，应用自绘最低 0.903585、外部区域最低 0.949065。五平台全量矩阵通过前不表述为全前端完成。
- 校园卡重构为严格只读的“余额票面 + 交易证据账本”：首页已有快照刷新时保留余额和详情入口，详情页接入按日期远端查询与全量只读同步，日期查询和全量同步共享单飞锁。复审后进一步冻结日期输入、“近 7 天”和两个远端行动，日期查询仅在成功后采用新范围，查询失败保留上次有效筛选、页码与用户新输入；查询/同步失败文案和恢复动词保持一致。仅 OA 账号或密码换代隐藏旧校园卡，无关邮箱/体育密码变化不再扰动本页；账户、父快照、服务和页面生命周期换代后旧请求不得回写，异常始终解除操作锁。补齐首页 operation-locked，以及详情 stale/error/partial-error/operation-locked/validation-error 和紧凑端恢复路径；终端错误不再展示无效余额，所有同步动作明确标注“只读”。视觉清单扩展为 160 个状态、1280 张设计参考；Windows 本批 104/104 张逐图达到 SSIM 0.90，最低 0.900665。由上一轮完整审计与本批最终报告逐图合并，当前 Windows 为 1156/1280，剩余 124 张历史失败，不能表述为全前端完成。
- 成绩详情、考试安排与过程化成绩统一重构为“教务证据账本”：成绩保留原始分值、学分和绩点证据，考试使用可本地切换正倒序的连续时间轴并保留待公布与临时说明，过程化成绩按课程展示课堂、作业与测验证据且不与总评混算。三页补齐 loading/content/empty/stale/error/operation-locked，筛选与刷新统一单飞；凭据、默认学期、父页快照或页面生命周期换代立即解除旧锁并隔离迟到结果，异常与失败保留当前范围和有效记录，动态下拉换代时关闭旧选项浮层，页面退出后 pending 完成不得回写。视觉清单扩展为 155 个状态、1240 张设计参考，完整 Chromium 核验生成 1240 张截图与 56 份外部区域 sidecar，SHA-256 全部一致。Windows 本批 144/144 张逐图达到 SSIM 0.90，最低 0.900468；完整 Windows 审计为 1079/1240，相对修正前 53 张 fail→pass、0 张 pass→fail，剩余 161 张主要集中在校园卡、校历、账户设置、第二课堂规则与 PDF，不能表述为全前端完成。
- 课程表重构为“每周学程地图”，紧凑端保留七天选择、当天列表、校历与文字刷新行动，宽屏保留完整七列空间关系。新增 initial/loading/empty/error/stale/operation-locked，刷新统一单飞并在失败时保留课程与星期选择；凭据、外部快照、缓存恢复和页面退出均按代次隔离，迟到的旧请求或旧账户缓存不得回写。视觉清单扩展为 149 个状态、1192 张设计参考，完整浏览器矩阵与 SHA-256 索引通过核验；修复全量参考生成器中旧交互原型抢占课表 content 基线的问题。Windows 本批 56/56 张逐图达到 SSIM 0.90，最低 0.900482。按 1136 张未变页面的全量审计与本批 56 张最终报告逐图合并，当前 Windows 为 952/1192，剩余 240 张，下一批优先收敛考试详情、成绩详情与过程化成绩。
- 第二课堂与体育考勤详情改为独立“学习证据账本”：完成度、积分/考勤证据和规则分层呈现，紧凑端使用完整字段记录卡并移除横向表格。两页刷新统一为单飞控制器，刷新失败保留现有证据，成功整体替换同代快照，页面退出或凭据代次变化后旧结果不得回写；错误页保留稳定恢复说明，并可从“更多操作”查看最近一次具体失败原因。新增加载、空、错误、缓存与操作锁定参考，视觉清单扩展为 148 个状态、1184 张设计参考，完整浏览器矩阵已生成 1184 张带 SHA-256 索引的设计候选并通过交互核验。Windows 本批 96/96 张逐图达到 SSIM 0.90，第二课堂最低 0.900653，体育考勤最低 0.909699；最终全量 Windows 审计实测为 913/1184，仍有 271 张历史页面待收敛，不能表述为全前端完成。
- 外部网页补齐主文档/运行时失败、外部打开确认与取消、浏览器拒绝、插件历史异常、旧 controller 隔离和操作锁定；工具栏与系统返回统一单飞并优先后退网页。外部打开异步结果绑定发起 URL 与文档代次，根路由退出失败后恢复网页历史拦截，重定向失败会原地重试失败请求地址，浏览到新地址会清除旧地址的外部失败。视觉清单扩展为 146 个状态、1168 张设计参考，阈值仍为逐图 SSIM 0.90；Windows 本批 48/48 张通过，最低 0.921257。由未变更的 1120 张完整审计与本批 48 张合并计算，当前 Windows 为 831/1168，剩余 337 张集中在尚未收敛的历史页面，不能表述为全项目前端完成。完成判定新增操作协同、原地恢复与设计稿外异常状态门禁。

## 0.4.0 · 全量状态参考完整性门禁

- 全局宿主与认证批次补齐首次协议选择的持久化锁定、保存失败与重试恢复场景；更多抽屉参考改用真实响应式清源导航壳。视觉清单现为 143 个状态、1144 张设计参考，逐图 SSIM 门槛保持 0.90。
- 根据最终验收要求，将应用自绘与外部区域的逐图 SSIM 阈值统一为 0.90；仍禁止按平均分放行。
- 教务总览重构为“学程账本”，新增部分失败、凭据全缺失、凭据部分缺失与协同刷新锁定四态；全局刷新协调五个既有只读来源，失败保留各区最后有效缓存，缺少凭据的来源不会被请求。Windows 十态、四档视口和亮暗主题共 80 张逐图按 0.90 验收；该批完成时视觉清单为 138 个状态、1104 张设计参考。
- SSIM 输入增加 6px 抗锯齿归一化，避免 Chromium 与 Flutter/Skia 字体边缘采样差异造成误报；差异热图继续使用原始像素，并新增布局位移反例测试。
- 快捷入口目录已对齐紧凑排版、任务行间距、外链角标与环形加载态；Windows candidate 四档视口与亮暗主题 32 张逐图均达到 0.95。
- 首页已对齐全状态紧凑排版、时间轨装饰圆、概览图标、陈旧缓存横幅与辅助坞等高布局；Windows candidate 四档视口与亮暗主题 40 张逐图均达到 0.95。
- 邮箱收件箱已对齐紧凑状态卡、环形加载、透明 neutral 状态、缓存横幅与移动邮件行；Windows candidate 六态、四档视口和亮暗主题 48 张逐图均达到 0.95。
- 邮箱撰写已对齐紧凑页首、地址字段断点、多行文本度量与发送中可读禁用态；Windows candidate 四态、四档视口和亮暗主题 32 张逐图均达到 0.95，最低 0.952726。
- 邮件正文详情已通过共享 reading 排版 token 对齐长文行高、段落节奏与弱化前景；Windows candidate 四档视口和亮暗主题 8 张逐图均达到 0.95，最低 0.954896。
- 校园资讯流已对齐六态固定高度面板、环形加载、紧凑来源药丸、两行标题与摘要节奏；Windows candidate 六态、四档视口和亮暗主题 48 张逐图均达到 0.95，最低 0.962316。
- 资讯来源筛选与空结果面板已改用无交互污染的确定性状态，保留空搜索框、默认来源和固定高度品牌空态；Windows candidate 两态、四档视口和亮暗主题 16 张逐图均达到 0.95，最低 0.961757。
- 外部网页确认页在内容与认证阻断两态下保持目标域名、返回路径和前置条件可见；Windows candidate 四档视口和亮暗主题 16 张逐图均达到 0.95，最低 0.971046。
- 法律声明、隐私说明和用户协议已迁移到结构化清源阅读框架，保留随包完整正文的可选择阅读、加载失败重试和主要行动；Windows candidate 三页、四档视口和亮暗主题 24 张逐图均达到 0.95，最低 0.954979。
- 外观设置已迁移到共享清源任务页框架，保留即时主题切换、来源说明和设置内真实入口；Windows candidate 四档视口和亮暗主题 8 张逐图均达到 0.95，最低 0.963115。
- 数据与隐私已迁移到流体任务账本，分离校园缓存、账户连接、隐私说明和全量清除边界；补齐状态读取失败、取消、执行中互斥、部分失败、完成明细与成功后刷新；Windows candidate 三态、四档视口和亮暗主题 24 张逐图均达到 0.90，最低 0.949397。
- 应用更新已从常规设置内嵌卡迁移到独立清源任务页，保留手动联网、下载取消、SHA-256 校验与平台降级；补齐检查/下载/打开互斥、取消后重试、缺少校验值、外部 Release 打开失败和安装入口失败；Windows candidate 四态、四档视口和亮暗主题 32 张逐图均达到 0.90，最低 0.907681。
- 微信公众号认证已从微信推文分区的多操作卡迁移到独立任务页，主分区只保留连接摘要；补齐操作互斥、扫码取消、凭据缺失、二次认证回滚、清除确认与失败恢复、WebView 主页面失败重试和结果回写竞态保护；Windows candidate 四态、四档视口和亮暗主题 32 张逐图均达到 0.90，最低 0.926286。
- 关于工大聚合已从设置内完整长页迁移到独立构建信息任务页，主分区只保留摘要；开源许可拆为窄屏卡片/宽屏表格的单一职责页。版本读取失败、外链确认/取消/失败/锁定均进入独立场景态，视觉清单扩展为 134 个状态、1072 张设计参考；Windows 两页 12 态、四档视口和亮暗主题共 96 张逐图均达到 0.90，最低 0.900284。
- 外部确认参考层新增 dialog 语义、标题关联、默认安全焦点、Tab 焦点循环、Escape 取消和触发器焦点归还的浏览器门禁。
- Android/iOS 视觉候选切换为对应模拟器上的 `integration_test` runner，候选图通过设备测试通道回传主机；不再使用宿主 `TargetPlatform` 模拟代替移动平台。
- 分段控制器落实 roving focus 与左右方向键切换；外观摘要只展示当前主题，避免与独立任务页形成重复编辑路径。
- 上述结果只证明 Windows candidate 对齐；正式完成仍以参考确认冻结及 Android、iOS、Windows、macOS、Linux 五平台矩阵通过为准。
- 新增独立于 Flutter 实现的 44 界面参考目录，补齐视觉清单全部 138 个页面/状态组合。
- 浏览器核验现在固定生成四档视口、亮暗主题共 1104 张设计参考候选，并写入逐图 SHA-256 索引。
- 五平台 Flutter 候选上传前必须通过文件名、PNG 尺寸和外部区域 sidecar 边界校验。
- 外部区域改为逐状态声明；PDF/WebView 加载与错误、系统认证说明等清源自绘内容不再错误使用 0.95 阈值。
- 次级参考稿按时间轨、课表、记录、邮箱、表单、设置、法律、文档和系统认证等页面结构分别渲染，不再复用单一卡片模板。

## 0.4.0 - 2026-07-23

- 补齐 123/123 视觉清单状态注册，覆盖设置、法律、WebView、PDF 与系统认证外部区域。
- WebView 与 PDF 抽取共享清源页面框架，外部正文以 sidecar 区域按 0.95 阈值独立验收。
- 设置新增可持久化的跟随系统/亮色/暗色外观选择，并补齐数据隐私、微信认证与更新四态。
- 补齐启动加载/错误恢复、首次协议、锁屏、关闭确认与响应式“更多”目的地的生产候选表面。
- 关闭确认统一使用延迟提交复选框、安全动作默认焦点和不可点遮罩退出的危险决策规则。
- ListItem 落实连续行、选中语义与 1px token 分隔契约，五个平台统一在 768dp 切换 NavRail。
- 冻结首页今日学程时间轨与校园服务概览的四档响应式设计。
- 增加 hero 排版、首页栏比、时间轨透明度与视口流体比例 token。
- 首页网络状态收敛为非操作型紧凑状态药丸，并明确移动端安全区契约。
- Ring 补齐 token 驱动的 activity 变体、加载语义和减少动态固定帧。
- QuickLink 补齐父卡片内 compact 变体、外链语义与确认边界。

## 0.3.1 - 2026-07-19

- 新增 `opacity.domainTint` 机器 token，统一业务域图标浅底的 CSS 与 Flutter 混色比例。
- 快捷入口新增外部网页确认页，固定目标域名、离开应用提示和 OA 登录会话缺失阻断状态。
- OA 外部入口门禁以可复用登录会话为准，不再把“已保存凭据”误判为当前登录态。
- 快捷入口错误态明确显示内置配置位置，并同步独立收藏、外部语义与认证门禁组件规格。
- 将布局尺寸 token 同步到 HTML 样例的 CSS 语义变量，并纳入漂移门禁。
- NavRail 在 medium 使用 80px compact 形态，在 expanded 起使用 220px extended 形态。
- 页面参考稿严格按 360×800、768×900、1200×900 和 1600×1000 视口输出，不再使用全页高度。
- 应用壳显式清除组件样例页的 `body` 外边距，确保导航与背景铺满视口。
- 课表 content 参考稿补全周一至周日，对齐固定脱敏数据和 09:30 时钟，compact 始终露出当天 Tab。
- 浏览器交互验证后强制重载原型，防止 Tab、筛选、邮件选中或设置分区污染默认 content 参考稿。
- HTML 参考稿显式加载 MiSans 四档字重，并校验排版字体 token，避免浏览器回退字体造成伪差异。
- 课表周视图在 medium 以上压缩为完整七列，CourseBlock 统一为业务域 tint，当前日整列使用青雾强调。
- 页面内容宽度、流式边距、状态药丸、NavRail、Tabs 与 IconButton 的 Flutter 契约同步到参考原型。
- 邮箱参考稿补齐收件箱 initial/loading/empty/stale/error、独立只读详情 content 与撰写 initial/content/loading/error，四档视口和亮暗主题均可确定性采集。
- 快捷入口参考稿补齐 loading/content/empty/error，保留任务分组、搜索无结果和明确恢复动作。
- 快捷入口目录补齐六个独立 48px 收藏按钮、按压语义与收藏反馈，避免迁移时丢失既有收藏能力。
- 快捷入口参考稿按配置语义固定教务、馆藏、校园卡、官网与认证图标，并使用对应业务域色。
- 参考稿采集前统一将鼠标移出内容区，防止 reload 后残留指针位置污染默认态截图。

## 0.3.0 - 2026-07-18

- 指定 `resources/tokens.json` 为唯一机器真源。
- 统一低饱和校园业务域色、36/28/22/18 字阶与 120/200/320ms 动效。
- 增加断点、控件尺寸、焦点环、输入框与磁贴圆角契约。
- 明确 Android、iOS、Windows、macOS、Linux 五个发布平台共享语义契约，但允许布局密度、导航形态和输入反馈适配；Flutter Web 不在本轮验收范围。
- 增加逐图 SSIM 验收工具：应用区域阈值 0.99，系统认证、网页和 PDF 等外部区域阈值 0.95，并在失败时输出实际图、基准图、差异热图和 JSON 报告。
- 明确清源组件与迁移期 Fluent 宿主的边界。
- 确立“今日学程时间轨”为首个纵向切片的视觉签名。
- 完成七个主目的地、详情边界与关键状态的页面级契约及响应式交互原型。
- 优化 compact / medium 设置分区、组内方向键导航、搜索空状态和页面行动反馈。

## 0.2.0 - 2026-06-16

- 建立清源总纲、基础令牌、44 份组件规格和对应 HTML 样例。
