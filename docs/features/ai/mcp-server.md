# 本地数据 MCP 暴露

> 模块：[AI 助手](README.md)　·　状态：**设计中**
>
> 与 [模型与数据边界](model-data-boundary.md) 同源同策的「反向」能力：AI 助手是**对内**调用本地数据；本功能是把本地**只读**数据通过 MCP **对外**暴露给外部 MCP 客户端。

| 项 | 内容 |
| --- | --- |
| 功能 ID | `ai.mcp-server` |
| 状态 | 设计中 |
| 平台 | **仅桌面（Windows · macOS · Linux）**；Android/iOS 不支持 |
| 关联 Issue | — |
| 主要代码 | 拟新增：本地 MCP server（HTTP/SSE）、只读工具/资源映射、授权与 Token 校验 |

## 1. 需求

将用户本地各域**只读**数据做成**可选**对外暴露的 MCP server，供外部 MCP 客户端（Claude Desktop、IDE、其它 Agent）按授权访问。权限、端口、绑定地址等可配；**默认关闭**。

**验收要点**
- 默认关闭，用户显式开启。
- 仅暴露**经授权的只读**数据；无写能力。
- 端口、绑定地址可配；**必须 Token 认证**。
- 移动端不支持（不提供 server）。

## 2. 传输与访问控制

- **传输**：本地 **HTTP/SSE**，**可配端口**。
- **绑定地址可配**：默认 `127.0.0.1`（仅本机），可改为局域网地址（用户自负责）。
- **必须 Token**：所有连接强制 Token 认证；Token 进系统安全存储，可重置。
- **默认关闭**：仅在用户开启且配置完成后监听。

## 3. 暴露内容与权限（复用按域授权矩阵）

- 暴露为 MCP **只读** tools/resources，映射各域只读数据（成绩/课表/考试/消息/校园卡等）。
- **复用** [模型与数据边界](model-data-boundary.md) 的按域授权矩阵：逐域开关决定哪些域可经 MCP 暴露；最小必要字段。
- **无写工具**：不暴露任何写操作。

```
MCP 客户端 ──HTTP/SSE(端口) + Token──▶ 本地 MCP server
                                          │ 按域授权校验（复用矩阵）
                                          ▼
                              各域只读工具/资源（最小必要）
```

## 4. 实现

- 桌面启动可配端口的 HTTP/SSE MCP server；监听前校验配置（端口/地址/Token 就绪）。
- 请求经 Token 认证 → 按域授权校验 → 调用对应只读工具 → 返回最小必要数据。
- 生命周期：随开关启停；调用可脱敏审计。
- 移动端：不实现 server，设置项显示「不支持」。

## 5. 关联

- 关联：[模型与数据边界](model-data-boundary.md)（共用按域授权矩阵与只读工具）；[设置中心](../platform/shell/settings.md)（开关/端口/地址/Token 配置）；[安全锁屏与隐私](../platform/system/security-privacy.md)（Token 安全存储）。
- 数据来源：各被授权只读功能（教务/资讯/校园生活等）。

## 6. 约束

- **默认关闭、必须 Token、只读**；无任何写能力。
- 绑定非回环地址属用户显式选择，需提示风险。
- Token 与暴露内容不进日志；数据对外暴露完全由用户开关与授权控制。
- 仅桌面；移动端不支持。

## 7. 待办与演进

- [ ] 桌面 HTTP/SSE MCP server（可配端口/地址）。
- [ ] Token 认证与安全存储、重置。
- [ ] 复用按域授权矩阵的只读工具/资源映射。
- [ ] 设置项（开关/端口/地址/Token）与移动端「不支持」表现。
- [ ] 调用脱敏审计。
