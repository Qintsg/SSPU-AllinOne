# 校园网（Network）

> 业务域：`network`　·　所属：[功能总览](../README.md)
>
> 当前保留校园网状态检测与受限服务门禁；#169 的一键 VPN 与本地插件方案已明确不再计划。

## 1. 模块职责

统一感知与处理设备的校园网络环境，并作为受限校园服务（教务、校园卡、邮箱等）的**访问门禁**：

- **状态检测**：当前使用 4 态双探针模型，作为全局门禁与顶栏/主页展示的共享数据源。
- **网络诊断**：对内网各服务逐项体检并测速，辅助排障。
- **VPN 一键连接 / 本地插件机制**：#169 已因权限边界和学校网络条件变化按 `not planned` 关闭，不再进入实现范围。

## 2. 功能清单

| 功能 | 状态 | Issue | 文档 |
| --- | --- | --- | --- |
| 校园网状态检测 | 部分实现 | — | [`network-status.md`](network-status.md) |
| 网络诊断与测速 | 设计中 | — | [`network-diagnostics.md`](network-diagnostics.md) |
| VPN 一键连接（仅桌面） | 不再计划 | #169（不再计划） | [`vpn-connect.md`](vpn-connect.md) |
| 本地插件机制（仅桌面，应用内不可见） | 不再计划 | #169（不再计划） | [`plugin-host.md`](plugin-host.md) |

## 3. 内部依赖

- 状态检测与诊断共享同一份网络状态。
- **被依赖**：教务、校园卡与课外活动等受限服务在请求前调用门禁。邮箱使用公网端点，不依赖校园网门禁。

## 4. 当前网络状态模型（4 态）

| 状态 | 含义 | 可达受限服务 | 可开启 VPN（仅桌面） |
| --- | --- | --- | --- |
| `campus` 校园网直连 | 处于校园网内 | 是 | 否（无需） |
| `vpn` VPN 环境 | 经学校 VPN 可达校园 | 是 | 否 |
| `outsideCampus` 校外 | 校园服务不可达 | 否 | 否 |
| `unknown` 检测中/未知 | 尚未完成或无法确定 | 否 | 否 |

当前没有插件 VPN 状态，也不能可靠区分“离线”和“有公网但校园服务不可达”。如后续需要扩展，必须另建 Issue，不能复用已取消的 #169 作为进行中任务。

## 5. 模块级约束

- **应用内 UI/文案零提及**插件、`plugins` 目录、外部来源或仓库（仅设计文档记录机制）。
- **门禁优先、网络探测不阻塞 UI**：检测异步，首检前为 `unknown`，不误报。
- **VPN 仅桌面、进程隔离**：应用不下载/不分发/不内嵌插件产物，由用户手动置入；独立进程运行使许可证（如 AGPL）保持隔离。
- **敏感信息零泄漏**：账号、密码、token、cookie、VPN 会话不进日志、状态文件、Release notes 或测试 fixture。
- 探针可注入，测试用 fake，不访问真实 `vpn.sspu.edu.cn` 与真实 VPN 账号。

## 6. 相关 Issue

#169（一键 VPN 与自动连接，已按 `not planned` 关闭）。
